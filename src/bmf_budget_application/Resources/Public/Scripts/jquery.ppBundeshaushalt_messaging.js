
jQuery.fn.ppBundeshaushalt.messaging = {

    // properties ################################
    mainDiv    : 'div.tx-bmf-budget',

    // methods ###################################
    'info' : function( header, content ) {

        // alert( header + "\n----------------\n" + content );

    },

    'error' : function( header, content ) {

        alert( header + "\n----------------\n" + content );

    }

}
